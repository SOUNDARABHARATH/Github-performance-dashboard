# from .nlp_processor import process_query

# def generate_response(query, metrics):
#     metric_name = process_query(query)
#     if metric_name:
#         return metrics.get(metric_name, "Metric not available")
#     return "Query could not be understood"
